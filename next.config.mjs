/** @type {import('next').NextConfig} */
const nextConfig = {
  // Projeto independente. Não altera as rotas do Ponto de Vista.
  agentRules: false,
  devIndicators: false,
};

export default nextConfig;
